"""
============================================
FIRST TIME? THOUGHTS → CODE
============================================

Paste a LeetCode problem + solution (or just the problem if you want the optimal approach discovered).

Produce a section titled:

    🧠 First Time? Thoughts → Code

Write as if I've never seen this problem. Trace how a human would think their way to the code — not just what each line does.

STYLE:
    • Concise, simple, easy to scan
    • Short bullets, skip lines between sections
    • No fluff
    • Be honest: call out what is NOT intuitive (weird syntax, tricks, patterns)

STRUCTURE (use these headings in order):

Read the problem (10 sec)
    • What are we being asked?
    • What constraint or hint in the problem statement matters?

Start naive (totally fine)
    • What's the obvious approach you'd say out loud first if stuck?
    • Time complexity in one phrase

The one insight that unlocks the optimal code
    • The single "aha" that changes naive → optimal
    • Why it works in plain words (2–5 bullets max)

Why [key technique]? (only if needed)
    • e.g. why two pointers, why fill backwards, why a hash map
    • Only include if the solution depends on a non-obvious choice

Thought → line of code
    • For each unusual or important line in the solution, map:
          variable / loop / pattern  →  what thought led to writing it
    • Focus on lines you wouldn't write on instinct (e.g. [0] * n, backward range, prefix[-1], deque, etc.)
    • One line of code → 1–3 short "→" explanations

Memory hook (one sentence)
    • One memorable sentence to recall the whole approach

Would you arrive at this cold?
    • What you'd get immediately without studying
    • What only appears after asking "what does the input structure buy me?"
    • Which code pieces are bookkeeping vs the real insight

RULES:
    • Do NOT ask clarifying questions — produce the section
    • Do NOT repeat a full line-by-line breakdown (that's a different prompt)
    • If I only paste the problem, derive the standard optimal approach, then write Thoughts → Code for it

---

PASTE BELOW:
Problem:
{{paste problem statement}}

Solution (optional):
{{paste code}}
"""
