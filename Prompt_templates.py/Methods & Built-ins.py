"""
You are helping me build and maintain a personal, scannable, frequency-ordered Python cheatsheet / quick-reference guide focused on the most useful built-in methods and functions for solving LeetCode-style Data Structures & Algorithms (DSA) problems at an entry-level SWE interview (non-FAANG companies, mostly Easy & Medium problems).

Please update / extend my existing guide using **exactly** the format and conventions shown below.

Rules you must follow strictly:

1. Organize into these exact top-level sections (in this order):
   # STRING METHODS
   # LIST METHODS
   # DICTIONARY METHODS
   # SET METHODS
   # BUILT-IN FUNCTIONS
   # SPECIAL METHODS

2. Use # ---------------------------------------------------------------------------------- to separate major sections.

3. For each individual method/function entry, use **exactly** this block format (do not change headings, order of lines, or comment style):

   # [SECTION TYPE]: [method or function name]()
   # What it does: [one clear sentence]
   # Why use it: [one clear sentence]
   # How it works: [one clear sentence]
   # When to use: [one clear sentence – focus on typical DSA/LeetCode use cases]
   # Time/Space: [realistic big-O in one line]

   # Syntax:
   [correct syntax line(s) – show variants if they exist]

   # Basic Example 1: [short title]
   [very short code + print comment with output]

   # Basic Example 2: [short title]
   [very short code + print comment with output]

   # Basic Example 3: [short title] (optional but preferred)
   [very short code + print comment with output]

   # DSA Example: [short descriptive title]
   [short realistic DSA-style code snippet + print comment with output]

4. Ordering inside each section: **Sort by real-world LeetCode frequency for entry/mid-level interviews** (most used first). Place commonly paired items near each other (e.g. .split() near .join(), ord() near chr(), .get() near .items(), etc.). Prioritize build/modify → validate → transform → debug flow.

5. When adding new items, tell me exactly where to insert them in the current order (e.g. "after .isdigit() and before .split()").

6. Keep descriptions concise, practical, and interview-focused. Avoid academic explanations. Use realistic DSA context in "When to use" and "DSA Example".

7. Never remove or reorder existing entries unless I explicitly ask. Only add or suggest better placement.

Current guide structure (paste your latest version here when you use this prompt):

[→ paste your entire current guide code block here ←]

Now, please add / update / reorder according to my request: [paste your specific request here, e.g. "add filter() and explain where to place it" or "reorder BUILT-IN FUNCTIONS by frequency" or "add heapq with examples"].

"""